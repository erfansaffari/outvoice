Pill chip for capabilities, finishes, certifications, and metadata.

```jsx
<Tag variant="teal">AAMA 2605</Tag>
<Tag variant="outline" size="sm">Anodized look</Tag>
```

Variants: `default` (cream fill), `outline`, `teal`, `navy`, `bronze`. Sizes `sm` `md`. Keep labels to a couple of words.
