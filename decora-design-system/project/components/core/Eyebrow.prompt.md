Small tracked uppercase section label — the brand's signature eyebrow.

```jsx
<Eyebrow color="accent">Voice fill</Eyebrow>
```

`color`: `accent` (teal), `muted`, `light` (on dark), `bronze`. Often paired with a short rule before the text.
