Dense square status badge (smaller and squarer than Tag) for statuses and states.

```jsx
<Badge variant="success" dot>In production</Badge>
<Badge variant="info">Spec ready</Badge>
```

Variants: `neutral` `success` `warning` `danger` `info`. Set `dot` for a leading status dot.
