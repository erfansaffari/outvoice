Editorial service/capability preview card — image over eyebrow + title + short copy + arrow link. Used three-up in the homepage services row.

```jsx
<ServiceCard
  index={1}
  eyebrow="Powder Coating"
  title="Durable colour, applied with control"
  description="Consistent film build and cure across high volumes."
  image="/assets/…/coating-line.jpg"
  cta="See finishes"
/>
```

Without `image` it renders a navy + dot-texture placeholder. Lifts and shows a teal arrow on hover.
