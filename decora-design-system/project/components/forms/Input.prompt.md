Single-line text input with label, hint, and error states.

```jsx
<Input label="Full name" placeholder="Jane Architect" />
<Input label="Email" type="email" error="Enter a valid email" />
```

Hairline border, square corners, teal focus ring. Pass `hint` for helper text or `error` for the error state.
