Native select styled to match the brand inputs, with a chevron.

```jsx
<Select label="Package" options={["Half Day — 4 hrs", "Full Day — 8 hrs", "Premium — 10 hrs"]} />
```

Pass `options` as strings or `{value, label}` objects. Teal focus ring, square corners.
