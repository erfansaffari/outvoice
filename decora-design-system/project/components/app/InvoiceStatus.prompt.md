Status pill for an invoice — draft / sent / paid / overdue, each with a colored dot. Self-contained.

```jsx
<InvoiceStatus status="paid" />
<InvoiceStatus status="sent" size="sm" />
```

Notes: pill shape, brand status colors (teal = sent, green = paid, stone = draft, terracotta = overdue).
